# githubro pi extensions

These TypeScript extensions expose narrowly scoped `gh` CLI operations to pi.
They read repository and issue context exclusively from the environment set by
the worker and return structured JSON results.
